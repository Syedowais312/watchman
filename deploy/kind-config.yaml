kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: watchman
# Default CNI disabled so Cilium can be installed as the CNI (required for Hubble flow data).
networking:
  disableDefaultCNI: true
nodes:
  - role: control-plane
  - role: worker
  - role: worker
